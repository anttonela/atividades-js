/*
    - String -

O tipo String do JavaScript é usado para representar dados textuais, funciona
entre aspas duplas ou simples.
*/

const fruta = "banana";

let nome = "André"
let sobreNome ="Silva"
let nomeCompleto = "Meu nome completo é : " + nome + sobreNome
let nomeCompleto2 = `Meu nome completo é ${nome} ${sobreNome}`;

console.log(nomeCompleto2);